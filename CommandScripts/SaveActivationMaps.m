function oOutput = SaveActivationMaps(sUnemapFilePath, sSavePath)
%This command script function saves the activation maps for a given unemap
%file to the specified path

%Load the specified file
oUnemap = GetUnemapFromMATFile(Unemap,sUnemapFilePath);

%Loop through the beats and produce 
oOutput = 1;
end

